<% error = @content.errors.key?(:title) && @content.errors[:title].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :title, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :title, :class => 'form-control input-xlarge input-with-feedback', :autofocus => true %>
    <span class='help-inline'><%= error ? f.error_message_on(:title) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @content.errors.key?(:slug) && @content.errors[:slug].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :slug, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :slug, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:slug) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @content.errors.key?(:body) && @content.errors[:body].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :body, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_area :body, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:body) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @content.errors.key?(:published) && @content.errors[:published].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :published, :class => 'control-label' %>
  <div class='controls'>
    <%= f.check_box :published, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:published) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @content.errors.key?(:type) && @content.errors[:type].count > 0 %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :type, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :type, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:type) : "Ex: a simple text" %></span>
  </div>
</fieldset>


<div class="form-actions">
  <%= f.submit pat(:save), :class => 'btn btn-primary' %>
  &nbsp;
  <%= f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue' %>
  &nbsp;
  <%= link_to pat(:cancel), url(:contents, :index), :class => 'btn btn-default' %>
</div>