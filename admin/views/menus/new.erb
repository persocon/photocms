<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:menus, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:menus, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :menu, url(:menus, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'menus/form', :locals => { :f => f } %>
 <% end %>
</div>
